import React from "react";

const Sound = props => {
  return (
    <div>
      <h2>{props.title}</h2>
      <div>Play Button Icon</div>
      <button>Edit</button>
      <button>Delete</button>
    </div>
  );
};

export default Sound;
